# Task P02 Engineering Handoff Note — Main UI/Controller

## 1. Environment & SHAs
- **Base SHA**: `1dfcf47d7d2d7156bd30415bd618155eaa44d4b4`
- **Target Platform**: Ubuntu 24.04 with KDE Plasma / X11 / Wayland (PyQt6 6.11.0, Python 3.12)

## 2. Owned Files Changed
- `src/imagesorter/ui_main.py`
- `tests/test_p02_contract_and_remediation.py`
- `docs/engineering/tasks/P02.md`

## 3. Implemented Contracts & Adapters
1. **Shared Operation Contract v1**:
   - Undo operations forward full `UndoToken` dictionaries to `worker.add_task(..., undo_token=last_action)`.
   - `on_operation_result` matches operation IDs and settles tasks idempotently once terminal results (`completed`, `completed_with_warning`, `failed`, `recovery_required`) are received.
   - Backward compatibility adapter for legacy `finished`, `error`, and `undo_record` worker signals retained and synchronized with canonical path matching.

2. **Shared Decoder Contract v1**:
   - `ImageLoader` handles current requests asynchronously.
   - Matches decoder emissions via `request_id` or generation counters (`load_generation`), discarding stale decoder payloads.
   - `QPixmap` instances are instantiated exclusively on the GUI main thread via `@pyqtSlot`.

3. **Shared Launch Contract v1**:
   - `MainViewer.__init__(settings_manager, initial_paths=None)` preserves single-argument construction while supporting optional transient paths.
   - `open_paths(paths: list[str])` loads transient input images without altering persistent source directory settings or destination rules.

## 4. Reproduced Behaviors Fixed
1. Full Undo token forwarding prevents file deletion on edited files.
2. Operation matching by ID settles tasks cleanly without relying on filename-prefix matching.
3. Custom trash move updates HUD/status and handles real worker events accurately.
4. Duplicate actions on pending files are blocked immediately; navigation skips pending items; empty/invalid source clears queue and sets `current_index = -1`.
5. Async cache miss rendering and GUI thread `QPixmap` creation implemented with zoom/fit preservation.
6. HUD wired for dimensions, file index, truthful background status, AI status, and Zen mode toggle.
7. Shortcut precedence matrix enforced (Level 0 System > Level 1 Nav > Level 2 Custom Hotkeys > Level 3 Fallback Letters) with input focus isolation.
8. Transient path opening contract implemented cleanly.

## 5. Test Verification Results
- **Pytest Suite**: 72 passed, 0 failures.
- **Ruff Linter**: 0 errors across codebase.
- **Screenshots**: Offscreen capture `hud_verification.png` recorded during pytest execution in fixture temp dir.

## 6. Integration Checklist & Unresolved Risks
- [x] All 8 reproduced behaviors verified with unit and integration tests.
- [x] Contract interfaces compliant with shared specification v1.
- [x] Zero-regression across existing test suite.
- **Unresolved Risks**: None within P02 domain. Cross-task dependency on extended worker/decoder signatures handled via transparent fallback adapters.
