# Archived Parallel Task Handoffs

`P00.md` through `P07.md` are point-in-time handoffs produced independently from
the pre-unification baseline. Their ownership boundaries, pending integration
notes, test counts, and capability claims are historical evidence—not the current
repository status.

Use the root README, `docs/architecture.md`, current tests, and
`docs/engineering/integration-status.md` for the unified implementation. The files
remain here to preserve reasoning and task provenance.
